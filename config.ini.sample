[local]
protocol = http
host = localhost:5984
user = {user}
password = {password}

[remote]
protocol = https
host = {cloudant_user}.cloudant.com
# Note: these may *not* be Cloudant API key/secret pairs if using backup.py as
# API keys do not have DB create priviledges
user = {cloudant_user}
password = {password}
